export interface FaceMeasurements {
  faceWidth: number;      // 顔の幅 (mm)
  eyeDistance: number;    // 目の間の距離 (mm)
  cheekArea: number;      // 頬の面積 (mm²)
  noseHeight: number;     // 鼻の高さ (mm)
  templePosition: number; // こめかみの位置 (mm)
} 